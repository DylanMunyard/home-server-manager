name: bfstats
description: Side project — Hetzner cloud
servers:
  - id:   bfstats
    host: ${HETZNER_HOST}
    user: root
    key:  ~/.ssh/hetzner
    passphrase: ${HETZNER_PASSPHRASE}
    ai:   Cloud VM running an API + UI stack on single-node k3s. Inspect workloads with kubectl.
